from setuptools import setup

setup(
    name="Prophet Wrapper",
    version="0.1",
    description="A simple wrapper for Prophet",
    url="https://github.com/",
    author="Randy Phoa",
    author_email="randy.phoa@ibm.com",
    license="MIT",
    packages=["prophet_wrapper"],
    # install_requires=["Django >= 1.1.1", "pytest",],
)
